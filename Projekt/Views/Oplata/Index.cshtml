@model IEnumerable<Projekt.Models.Oplata>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.stawka_za_dzien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dni_w_miesiacu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Miesiac.nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rok.rok1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.stawka_za_dzien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dni_w_miesiacu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Miesiac.nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rok.rok1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
